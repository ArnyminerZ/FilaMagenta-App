/**
 * The key in localStorage where the authorization is stored.
 * @type {string}
 */
export const STORAGE_TOKEN = 'TOKEN';

/**
 * The key in localStorage where the cached profile information is stored.
 * @type {string}
 */
export const STORAGE_PROFILE = 'PROFILE';
